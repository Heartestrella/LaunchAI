# Applio 3.6.2 (IAHispano/Applio,LaunchAI 的 RVC 实现)
# pip_worker.py:800-814 流程:
#   1. _filter_torch_requirements 把上游 requirements.txt 里的 torch* 行剥掉
#   2. pip install -r requirements.txt
# 这里 lock 的版本是 py311 当前 freeze 实际状态——注意有几处和上游
# requirements.txt 不一致(被后续 GPT-SoVITS 等工具的依赖解析调整过):
#   - librosa: 上游钉 0.11.0,实际是 0.10.2 (GPT-SoVITS 钉 0.10.2)
#   - gradio:  上游钉 6.5.1,  实际是 4.44.1 (GPT-SoVITS 钉 gradio<5)
#   - numpy:   上游钉 2.3.5,  本 lock 改成 1.26.4 —— 见下方注释
#   - requests: 上游约束 >=2.31,<2.32, 实际是 2.34.2
# 整合包要还原"装完所有工具后的 py311 状态"就以本文件为准;
# 单独装 Applio (不带 GPT-SoVITS) 建议改回上游约束,否则 librosa/gradio
# 主版本号差异可能影响 Applio 自身。

setuptools==81.0.0
# numpy 锁到 1.26.4 而不是上游钉的 2.3.5 / freeze 时的 2.4.6 —— 因为
# whisper 用的 numba 0.63 兼容上限是 numpy 2.3,装 Applio 时如果把 numpy
# 拉到 2.4+,whisper 立刻挂(ImportError: Numba needs NumPy 2.3 or less)。
# 这一行必须与 whisper.txt / audiocraft.txt / gptsovits.txt 同步。
numpy==1.26.4
requests==2.34.2
tqdm==4.68.3
wget==3.2

ffmpeg-python==0.2.0
faiss-cpu==1.13.2
librosa==0.10.2
scipy==1.16.3
soundfile==0.12.1
noisereduce==3.0.3
pedalboard==0.9.23
stftpitchshift==2.0
soxr==1.1.0

numba==0.63.1
torchcrepe==0.0.24
torchfcpe==0.0.4
einops==0.8.2
transformers==4.44.2

matplotlib==3.10.8
tensorboard==2.20.0
gradio==4.44.1

tensorboardX==2.6.5
edge-tts==7.2.7
pypresence==4.6.1
beautifulsoup4==4.15.0
sounddevice==0.5.5
webrtcvad-wheels==2.0.14
